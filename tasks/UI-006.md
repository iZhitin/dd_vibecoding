# Task Report: [UI-006] Создать страницу Review

## Статус: Выполнено

## Описание выполненных работ
1. **API**: 
   - Созданы схемы ответа `PracticeLogReview` и `PracticeSessionReviewResponse` в `backend/app/schemas/practice.py`.
   - Реализована логика выборки информации для обзора сессии `get_practice_session_review` (с джоином таблицы `cards`) в `backend/app/services/practice.py`.
   - Добавлен новый эндпоинт `GET /api/practice/sessions/{session_id}/review` в `backend/app/api/routes/practice.py`.
2. **Frontend**:
   - Обновлен клиент API (`frontend/src/api/practice.ts`) для поддержания нового метода обзора сессий.
   - Разработан компонент цветового индикатора `TrafficLight` (`frontend/src/components/TrafficLight.tsx`).
   - Разработана страница обзора сессии (`frontend/src/pages/ReviewPage.tsx`), которая загружает данные по завершенной сессии и отображает их в визуальном формате со цветовой дифференциацией.
     - Индикация `GREEN` (Correct), `GREEN_STAR` (с похвалой), `YELLOW` и `RED` (исправления и объяснения).
3. **Проверки и Тестирование**:
   - Выполнен запуск `ruff check app/ --fix` (форматирование).
   - Выполнен запуск тестов pytest (32 теста успешно пройдены).
   - Выполнен запуск `npm run lint` во фронтенде (линтинг успешно пройден).
4. **Документация**:
   - Задача отмечена как выполненная в `BACKLOG.md`.

## ADR
Нет новых решений для данного таска (использовались ранее определенные схемы и архитектурные подходы).
