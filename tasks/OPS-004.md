# [OPS]-[004] Настроить HTTPS и email-аутентификацию домена

## Что было сделано:
1. Заменил базовую конфигурацию Nginx в `nginx/nginx.conf`:
   - Настроен 80-й порт для редиректа на HTTPS.
   - Добавлен rule `/.well-known/acme-challenge/` для `certbot`.
   - Настроен 443 порт со ссылками на сертификаты.
2. В `docker-compose.yml` добавлен сервис `certbot` для автообновления SSL-сертификатов (обновление каждые 12 часов).
3. Добавлены volumes `certbot_certs` и `certbot_data` в `docker-compose.yml` и примонтированы в `nginx` и `certbot`.
4. Создан `README.md` с подробными инструкциями:
   - Как настроить A-запись DNS.
   - Как настроить DKIM, SPF, DMARC для успешной доставки email через Resend.
   - Пошаговая инструкция первоначального выпуска сертификатов с помощью `certbot`.

Все тесты были успешно запущены и пройдены.
