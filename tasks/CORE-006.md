# Отчёт по задаче CORE-006

## Выполненные действия
- Добавлена схема `CardTranslationRead` в `app/schemas/card.py`.
- В `app/services/card.py` реализована функция `get_card` для извлечения карточки по `user_id` и `card_id`.
- Добавлен защищённый эндпоинт `GET /api/cards/{card_id}/translation` в `app/api/routes/cards.py`, который возвращает перевод карточки в формате JSON.
- Написаны unit-тесты в `tests/test_cards.py`, которые успешно проходят. Покрытие тестов включает успешное получение перевода и обработку случая ненайденной/чужой карточки (статус 404).
- Маршрут защищен зависимостью `Depends(get_current_user)`, неавторизованный пользователь получит 401.
- Задача [CORE]-[006] отмечена как выполненная в `BACKLOG.md`.
- Добавлена запись о выполнении задачи в `CHANGELOG.md`.

## ADR
Новые архитектурные решения в рамках данной задачи не принимались.
